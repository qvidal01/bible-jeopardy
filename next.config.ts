import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // React Compiler requires babel-plugin-react-compiler
  // Enable when the package is installed:
  // reactCompiler: true,
};

export default nextConfig;
